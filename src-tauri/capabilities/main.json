{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-export-dialog",
  "description": "Allow the local FastPAS main window to choose an export destination.",
  "windows": ["main"],
  "permissions": ["dialog:allow-save"]
}
